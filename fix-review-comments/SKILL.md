---
name: fix-review-comments
description: >-
  Use BEFORE the first edit of any round of review-comment fixes, and whenever a round produced new
  findings, a red CI, or a reopened thread. Also when the reviewer says a test does not exercise the
  change / only asserts that something was refused / stays green with the fix reverted; when the same
  defect turns out to be live at another call site, surface, DTO doc or cookbook part; when a file is
  on its third round; **whenever you are about to write a comment, a commit line or a self-review row
  that EXPLAINS why you took a shortcut**; or when the user asks "为什么改完又有新问题" /
  "是不是我们刚才的改动导致的" / "怎么又是一堆 comment" / "每次改动你都会引入新的问题".
  Produces the four pre-edit artefacts a fix round needs, states what each kind of green is worth,
  and says when to stop editing and write to the reviewer instead.
---

# 修评审意见,而不是把问题越修越多

不做分档(`pr-self-review`),不审别人的 PR(`pr-review`)。**这个技能只管:已经决定要修了,怎么改才不会带出新的。**

一次真实统计:五个 PR、18 轮评审,**11 轮里有"上一轮修复引入"的发现**。最严重的一次是插进 if 链里的一条语句把后面的 `else` 改嫁了——工具对所有普通调用都失效,而八条测试全绿、编译门全过。

**而评审一次软件都没跑过。** 没有活的宿主、没跑 pytest、没做 E2E——和你条件完全一样。那条 BLOCKER、
写进不会被序列化的字段的提示、指向装不下答案的参数的追问,全是**读出来的**。所以"本机跑不了行为测试"
不是借口:下面第 ③ 步那张表是免费的,而它抓住的正是这一类缺陷。

---

## 第零屏:一轮的顺序(顺序本身就是内容)

这四步散着放、没有一份文件写明顺序时,**每一轮都会被重新发明一次,而且每次都不一样**。

### 1. 先重新拉一次线程。现在。

> **永远。** 这一轮做的第一件事是**重新拉取**这个改动的评论线程,即使同一个会话里刚拉过。
> 绝不相信之前那次"零条评论"的结果。

评审是在第一次拉取之后才落地的(人工评审,或者单独发帖的自动评审),所以一次过期的读取会让你
得出"没事可做"的结论,而一整轮正在等着。

> **出事经过**:早先一次拉取只看到系统线程,于是"没有评论"被缓存了。用户重跑这一轮说"去把它们
> 都处理了","它们"被读成了之前列出的待办任务,好几轮对话花在起草证据笔记和评估不相关的仓库上。
> 用户把话拉回来:*"why not addressing comments under PR 58523? why asking me post QE notes?"*
> **重拉一次,冒出六条新的评审线程。**

含糊的"把评论都处理了"指的是**这个改动上活着的线程**,不是你之前列出来的任务清单。真觉得用户指的
是别的,用一句话问,不要按错的"它们"往下走。

**注意第二个评审人中途到场。** 有一次第 2 轮是用户自己的评审文件,第 3 轮是单独发帖的自动评审;
把推送压住、等线程重查一遍,才让所有东西在一次推送里落地。

### 2. 动手之前,先把所有发现列成表

拉完立刻把**全部**发现列出来,**在任何验证或修改之前**。

| # | 线程 / file:line | 评审定的级 | 发现 | 计划裁决 |
|---|---|---|---|---|

计划裁决只能是这五个之一:

| 裁决 | 含义 |
|---|---|
| **Accepted** | 发现对,修法也对 |
| **Accepted, fix corrected** | 发现对,但它给的修法不对 |
| **Accepted, scope corrected** | 是真问题,但属于另一条工作线 |
| **Rejected** | 带着证据,写在表里 |
| **Deferred** | 只在用户明确裁定时才可以 |

**为什么**:用户要在**工作发生之前**看到整体地貌和你打算接受/拒绝什么,好在动手前掉头,而不是
做完之后。先写一段散文再直接开修,会把这一轮的形状藏到快做完才显形。

### 3. 修,在提交门上

### 4. 推送和回帖**一起**发出去

> **永远。** 批准之后,**在同一步里**推分支 + 发回帖。绝不先把修复推上去、回帖留到以后。

**为什么**:评审应该同时看到修复落地和解释它的回复。先推后回会把这一轮劈开,可能让线程回答的是
一个评审没见过的分支状态。用户在一次"先推了五个修复提交、之后才发六条回帖"的轮次之后说:
*"in the future, push fixes should be together with post replies."*

---

## 第一屏:动手改之前,先产出四份东西

写进 scratchpad,**不是在脑子里过**。四份都写不出来,就还不该改代码。

### ① 轮次账 —— 这一轮该不该由我改代码

```bash
git diff --name-only <上一轮的提交>..HEAD
git log --oneline <本轮分支起点>..HEAD -- <这次要改的文件>   # 这个文件被改过几轮
```

- **同一个文件第三轮**:停。不要再改。写一段给评审:"前几次修在了错的位置,规则真正该待的地方是 X",然后重新分档。
- **这一轮的改动超过 ~50 行,或者动到了规则/引擎本身**:这已经不是修 comment,是重构。降级回 `pr-self-review` 全流程(依赖枚举、约束句、和评审重新划范围),不要用轻量流程。

> 实测:`OptimisationConfigRules.cs` 连续四轮被改,最后一轮 +249 行。没有任何机制拦住它——因为"同一文件三轮就冻结"这句话写在技能的**末尾**,而技能是在**动手前**读的。所以它现在是第一条,而且是一条命令。

### ② 表面清单 —— 同一个问题有几个地方要回答

**这是本次代价最大的一类(18 条发现)。** 输入形态表回答的是"一个地方会被哪些输入打到";这一步回答的是"这个问题有几个地方在回答它"。

```bash
grep -rn "<规则名/标识符/那句文案>" --include=*.cs --include=*.md .    # 两个仓库都要
```

一个行为规则在这类代码库里通常有 **三到六个家**:

| 表面 | 典型例子 |
|---|---|
| 写入口 | `CreateXxxCommand` |
| **第二个写入口** | `UpdateXxxCommand` —— 重写同一份数据,校验却只有一半 |
| 查询口 | `Describe` / `CanRun` —— 必须用**和拒绝时同一句话**回答 |
| 运行口 | 真正执行的那条路 |
| 第二个读者 | 桌面 ViewModel 和 DTO 映射读同一条记录 |
| 契约文档 | DTO 的 XML doc,以及它**兄弟成员**的 doc |
| 语料/手册 | cookbook part、schema description、file header |

**全部列出来,一个提交里一起改。** 漏掉任何一个,下一轮它就是新 comment。

> 实测:运行口加了拒绝、查询口没加 → 多一轮;SDK 结果里 DAS 天线不报频道、桌面表格还在报 → 多一轮;创建口校验了白名单、Update 口没校验 → 我自己 grep 出来才没多一轮。

### ③ 输入形态表 —— 退化行留空就是下一轮的 comment

列出**所有**能到达这段代码的形态,逐个手走,写下行号和结果。**下面这几行是预印的,必须填**:

```
形态                     走到哪    结果
空 / 一个都没有
恰好一个            ← 最高产:一个候选、一个频道、一个系统
全都一样 / 全都关掉
已经是目标状态了
被锁定 / 不可动
取消 / 中途失败
（再加这次改动特有的：策略 × 数量 × 命名空间 × 实体集合 的组合）
```

**这张表要落进提交信息或回帖里。** 写不出来 = 没验证;只有你关心的那一行打勾、其余空着 = 下一轮的 comment。

> 实测:这一批里有三轮完全是退化情形——"只有一个候选频道就不该叫选择"、"全向天线没有方向可言"、"DAS 天线根本没有自己的频道"。
>
> 还有一条更隐蔽的:**判定式不能用计数代替事实**。"有没有东西被钉住"(数 allow-list)代替了"这次要动的每个系统是不是都被钉住"——编译过、构建过、文本断言过、连围着它写的测试变异都过,整条绿色阶梯对这个缺陷是瞎的。**凡是写 count / length / Any / 存在性判断,先问一句:我数的这个东西,和我真正要保证的事实是同一件事吗?**

### ④ 包住编辑点的整个构造 + 出口 + 往返

- 把整条 if/else 链、整个方法、整个 using 块读完再动。**插入点紧跟着的下一条语句是什么?** 有没有 `else` / `catch` / `finally` 会改嫁给你。
- **出口**:我写进去的值,谁把它发出去?那条路径真的会执行到吗?(提示写进了错误信封不序列化的字段 = 用户什么也看不到)
- **往返**:我让对方回答的东西,回来的形状装得下吗?(把答案指到一个数组参数上,而表单合并回来的是标量 → 烘焙成空数组 → 死循环)

---

## 第二屏:改的时候

### ⓪ 辩解句检查 —— 我写下的每一句理由,都是下一条 finding 的位置

**一轮 4 条发现,3 条头上顶着我自己写的一句辩解。** 这不是"不够仔细",是一个有形状的动作:
**一旦为某个折中写下理由,我就停止攻击它了**——而那句理由恰恰是下一条 finding 待的地方。

| 我写下的辩解 | 它出现在哪 | 真相 | 后果 |
|---|---|---|---|
| "SystemTemplate 是唯一的 public 入口,字段名是 ASM 内部的" | **代码注释** | `TemplateFieldNames.SystemID` 是 `public const`,全库 18 处在用 | CRITICAL 515374 |
| "前置读同样解析" | **提交信息** | 另一个门写的是 `?? building_name`,把缺陷原样装回去了 | BLOCKER 515370 |
| "消息里两个都提了,所以是 NIT" | **自审表** | 那个答案修不好任何东西,还能被选第二次 | CRITICAL 515371 |
| "这个目录里从来没有整数走过 chooser" | 差点写进注释 | `azimuth_deg` / `target_dbm` / `x,y,z` 都走过,`arg_baking.as_number` 就是为这个往返写的 | 结论碰巧对,理由是编的 |

**规矩:凡是写下一句"为什么这样就行/为什么只能这样",立刻用一条命令去攻击它,不许记录完就走。**

| 辩解的形状 | 攻击它的命令 |
|---|---|
| "只有这一条路 / 没有 public 的 X" | `grep -rn "X"` 整库。**一个编译错误只证明你试的那条不通,不证明没有别的路。** |
| "另一处也是这样处理的" | `git diff` 两处,或 `sed -n` 把两边并排贴出来逐字比 |
| "这个目录/仓库从来不 Y" | `grep -rhon 'Y' <目录> \| sort \| uniq -c` —— 让计数说话 |
| "这个答案够用了" | 问一句:**用户照这个答案做一遍,请求会变得能通过吗?** 不能 = finding,不是 NIT |
| "这样更安全所以加了 try/catch" | 去读被捕获的那个调用真的会不会抛。**兜底的代价常常比它防的事大**(这次那个 `catch` 什么都防不到,而它包住的那个 indexer 会往用户的模板库里**写**一条参数) |

> 这条和第 ① 步的"第三轮就冻结"是一对:轮次账拦的是**改太多次**,辩解句检查拦的是**一次改得太自信**。

### 三条读法

1. **作者读"我改的那条路通了吗",评审读"我没改的那些路现在怎样"。** 回归永远在你没想到的那条路上。
2. **顺着数据往外读,直到它离开系统**,并且**同一个问题的每个表面必须给出同一个答案**(见 ②)。
3. **改措辞之前 grep 整个仓库**:你自己上一轮写的断言、别人的测试、cookbook,都可能钉着旧措辞。

### 绿色阶梯 —— 以及回帖前**必须**爬到哪一级

| 绿 | 证明了 | 没有证明 | 回这种严重度之前至少要到这级 |
|---|---|---|---|
| 解析 / py_compile | 能解析 | 任何行为 | — |
| 编译通过 | 类型能解析 | **控制流**(`else` 改嫁是合法 C#) | — |
| 构建通过 | 能产出程序集 | **测试会不会红** | — |
| 源码文本断言 | 某段文字在文件里 | **执行路径** | MINOR(且要说清这是文本断言) |
| 手走输入形态表 | 控制流 | 真实数据下的数值 | MAJOR |
| 变异检查 | 这条测试真钉住了这个修复 | 别的没被打破 | **CRITICAL / BLOCKER** |
| 真跑一遍 / 真执行的门 | 行为 | — | 写"已验证"三个字的唯一门槛 |

### 修复轮的测试契约(四条,全都是硬的)

1. **走产品入口**,不走你刚抽出来的 helper,也不自己 new 出中间值——否则生产那一半的接线没人钉。
2. **断言"到达的值",不要只断言"拒绝触发了"。** 这一批里最贵的教训:证明"请求不再被拒绝" ≠ 证明"频道真的落下去了"。两次都是评审替我发现的。
3. **变异检查,而且要按"别的作者会怎么写这个修复"去还原**,不是按你自己怎么写的去还原。锚点
   **从文件里粘贴**,不要凭记忆写——凭记忆写的锚点会让测试挂在自己的 assert 上,而不是挂在门上
   (一轮里发生过两次)。具体的漏网形态:只断言某个 `if` 的**位置**、不断言里面的 `return;`;
   `rindex("if (…)")` 在 `else if (…)` 里**也命中**,所以还原成 `else if` 之后测试仍绿——要收紧成
   "它前面那个词不能是 else"。
4. **断言贴着被测的块**,不要整文件否定断言(会咬到你自己解释用的注释)。
5. **夹具/harness 打印出来的每一个字段都要断言,不许只挑自己关心的那个。**

> 实测:我写的执行 harness 每个形态打印 `{problems, needs, regions}`,我只断言了 `problems` 和
> `regions`。"对的楼、空的层"那一行**当场打印出 `needs:["building_name"]`**——证据就在屏幕上,
> 测试全绿,评审把它提成了 CRITICAL(515371)。**打印了却不断言的字段,等于没测。**
> 顺带:让 `Need` 桩连 options 一起吐出来(`arg + "=" + join(options)`),否则"提了一个没用的选项"
> 这类缺陷在断言里根本不可见。

**变异编译通过是常态,不是例外。** 可选参数(`IReadOnlyList<int> x = null`)删掉实参照样编译,结果静默变空。**构建绿在这里等于没验证。**

### 还要验证"验证器"本身

写了一个专门用来兜底的检查(桩保真检查、门、lint 规则)?**对它也做一次变异。**

> 实测:我写了个反射真 DLL、逐个比对桩里类型的检查,它绿了,我把它当"弱点已覆盖"写进了回帖。实际上解析器读出了类型却在传给探针前丢掉了——把 `Guid` 改成 `string` 它照样通过。**只有变异检查器本身才暴露出来。**

---

## 回帖:证据的级别和范围都不能虚

- **每个数字都必须来自在"已推送的那个 head"上跑过的命令**,并在回帖里点名是哪条命令。构建绿永远不是关于测试的证据。
- **范围也要写**:回帖里要有一行"这个规则涉及的每个表面/调用点:…"。只说"在 X 修好了"而同样的缺陷在 Y 还活着,是这一批里反复发生的事。
- **上一轮的说法复现不了,就在评审发现之前自己更正。** 我这次有两处:一处是"编译门通过"(机器上根本没有那个构建),一处是数字过期(42/42 → 50/50)。自己纠正的代价远小于被抓。
- **拒绝一条约定类意见时,第一次回复就把字节级证据摆上**(几个同目录文件有 BOM、loader 用的什么编码),不要拖到第二轮。
- **约定是按目录算的,不能跨仓库套用。** 同一条 BOM 意见:tool-engine 里 41 个文件 0 个有 BOM(该拒绝),iBuildNet 里 13 个文件 12 个有(该照做)。
- **评审引用了不存在的文件/代码,不等于论点错。** 引用和论点分开核。

### 逐条 verdict 清单是强制的

**报告结尾一条评论一行,行数永远不少于评论数。** 按**评审自己的顺序**,每行带:线程 id 或
file:line、评审定的级、几个词说清发现、以及上面那套固定词汇里的一个裁决。

一段散文总结**不能**代替它。散文正是一条发现被悄悄丢掉的地方——那条不方便的、那条你只修了一半的、
或者那条你太爽快同意以至于忘了说的。**把行数和评论数对一遍**,漏掉就看得见了,先对你自己看得见。

两条让清单诚实而不是装饰的规矩:
- **没有证据的裁决只是意见。** 每一行只要不是干脆的 "Accepted",就要点名 file、line 或那次测量。
- **裁决按评论算,不按提交算。** 一个提交可能回答三条发现,一条发现可能要三个提交;清单跟着**评审的
  编号**走,好让他勾自己的那份清单,不是勾你的。

## 三种"不算解决"

- **把缺陷写成注释里的"已知限制"。** 如果那句话描述的是用户会认为错的结果,它就是一条 finding。(我写过"One limit, stated rather than hidden…",那句话原文就是评审下一轮提的 CRITICAL。)
- **"历史遗留 / 超出范围"** —— 用之前先看工作项:这一批里有一条被连续两轮以这个理由关掉,第三轮才发现它就是工作项报的那个缺陷本身。
- **症状没追到就 resolve。** 只做了一半、或者选了三个方案之一的,留 active 并写清楚。不可争议的(措辞、大括号、评审指定的一条测试)修完直接 resolve,免得评审再点一遍。

---

## 每轮结束前的检查清单

```
git diff <上一轮的提交>..HEAD
```

1. **表面清单全打勾了吗?而且是在同一个提交里?**
2. **输入形态表的退化行填满了吗?**
3. 我插入的每一段,紧跟其后是什么?有 `else` / `catch` / `finally` 改嫁吗?
4. 我新引入的每个名字,在作用域里唯一吗?(外层 `var matches`,内层循环里早有一个 → CS0136)
5. 我改掉的每个字符串 / 异常 / 分支,**谁依赖它**?grep 过测试和调用方了吗?
6. 判定式有没有用计数/长度/存在性代替真正的事实?
7. **这一轮我写下的每一句辩解(注释 / 提交信息 / 自审表),都跑过攻击它的那条命令了吗?**
8. 行为改了,**描述这个行为的那句话**跟着改了吗?(这次:卡片改成问楼层,提示语还写着"说出楼栋和楼层")
9. 这一轮加的每条测试:走产品入口了吗?断言的是到达的值吗?**它打印的每个字段都断言了吗?**变异过吗?
10. 改了行为,**契约三件套**(规则代码 / DTO XML doc / cookbook part)都改了吗?**兄弟成员的 doc** 呢?
11. 用户可见的文案改了吗?五语资源跟上了吗?
12. 生成物/跨仓库的配对 PR(语料重发)要不要跟着动?合并顺序对吗?
13. 回帖里每句证据对应哪一级?范围行写了吗?
14. 要 resolve 的每一条,症状真的没了吗?

**一个 finding 一个提交**,提交信息写"行为变成了什么",不写 comment 编号——否则第 0 步那个 `git diff` 没有边界可用。

## 什么时候停下来改用写的

| 触发 | 下一步不是编辑,而是 |
|---|---|
| 同一个文件第三轮 | 给评审写"前几次修在错的位置,规则该在 X",重新分档 |
| 这一轮改动 > ~50 行 / 动到规则引擎 | 降级回 `pr-self-review` 全流程再推 |
| 严重度回升(MINOR → BLOCKER) | 把自己这几轮的 diff 当别人写的从头读一遍 |
| 没约定合并线 | 和评审约:BLOCKER/CRITICAL 清零,MAJOR 修或挂工作项,MINOR 作者裁量 |

## CI 在你修完之后变红,先分类再动手

1. **是不是已知抖动?** 本仓库有一类:`ReportEXTest` 约 25 条因 Measurement Templates 文件锁失败——不是你的改动,用 `ado_pr.py requeue <pr>` 重排。
2. **是不是你自己的测试?** 修复拿掉了某个"恰好成立"的东西,而旧测试正靠它通过(`Assert.Catch` 要求必须抛,你把越界修好了 → 红)。修复和它的测试要一起改。
3. **是不是 target 漂移?** 评审的意见可能写在更老的 merge base 上,或者 develop 中途合进来带了新 lint 规则。**每轮开始前先合一次 target,并读 target 新增了什么。**

## 工具坑(踩一次就够贵的)

- **多行代码用 Write 工具写,不要用 heredoc。** `\n` 会塌成真换行、引号会让 heredoc 提前结束——本次会话三次。
- **`%` 格式化含 `%` 的内容会炸**(body 里有 "95%")。用拼接。
- **管道的负面结果必须先证明工具跑过了。** `strings x.dll | grep -c Sym` 在没有 `strings` 的机器上静默输出 0,我据此断定"这个 DLL 没有那个符号"——错的,而且基于它的回帖也错了。
- **读退出码要直接读**,`grep -c` 无匹配退出 1,会把成功的构建显示成失败。
- 变异的具体执行、批量与还原安全,见 `mutation-verify`。

## 相关

- `pr-self-review` —— 分档、依赖枚举、约束句、维护类扫描、回复/resolve 策略。**本技能不重复这些,需要时按名引用。**
- `mutation-verify` —— 变异检查的执行工具与安全规则。
- `pr-review` —— 同一套眼光用在别人的 PR 上;严重度定义在 `pr-review/REVIEW.md`。
